<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- ✅ Permissions (นอก <application>) -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- ✅ Android 13+ ต้องขอสิทธิ์แจ้งเตือนแยก -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- ❌ ยังไม่ใส่ ถ้ายังไม่จำเป็นจริง ๆ
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    -->

    <application
        android:name="${applicationName}"
        android:label="ednudge"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config">

        <!-- Google Maps/Places (ถ้าใช้ Map/Geocoding) -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyDqsAi8eVba8JV_VJekrt4BozrkMGWfjDk" />

        <!-- ช่องแจ้งเตือนดีฟอลต์ (ต้องสร้าง channel ตรงชื่อเดียวกันในโค้ดด้วย) -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="edunudge_alerts" />

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode">

            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

    <!-- Queries (ตามที่ Flutter ต้องใช้) -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT" />
            <data android:mimeType="text/plain" />
        </intent>
    </queries>

</manifest>
